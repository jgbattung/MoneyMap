<plan feature="installment-management-qa-fixes">

  <phase name="Phase 1 — API bug fix (Fix 5)">

    <task>
      <name>Fix balance reversal in DELETE /api/expense-transactions/[id]</name>
      <action>
Open `src/app/api/expense-transactions/[id]/route.ts`. Locate the `DELETE` handler (around line 318).

Inside the block that runs after the `if (existingExpense.isInstallment) { ... return 400 }` early-return (the comment is `// Regular expense or payment record: Hard delete`, currently around line 358), remove the `if (!existingExpense.isSystemGenerated) { ... }` guard entirely. Always push the balance-reversal `db.financialAccount.update` into the `operations` array.

Resulting shape (verbatim target):

```ts
// Regular expense or payment record: Hard delete
const operations: PrismaPromise<unknown>[] = [];

// Reverse the balance deduction (applies to all expense transactions
// reaching this path — regular expenses AND system-generated child
// installment payments, both of which decremented the balance on create).
const amountToReverse = parseFloat(existingExpense.amount.toString());

operations.push(
  db.financialAccount.update({
    where: {
      id: existingExpense.accountId,
      userId: session.user.id,
    },
    data: {
      currentBalance: {
        increment: amountToReverse,
      },
    },
  })
);

// Delete the expense record
operations.push(
  db.expenseTransaction.delete({
    where: {
      id: id,
      userId: session.user.id,
    },
  })
);

await db.$transaction(operations);

await onExpenseTransactionChange(existingExpense.accountId, existingExpense.date);
```

Do not touch the parent-installment early-return block above it. Do not touch anything below `await db.$transaction(operations);`.

**Test updates in the same file's sibling test:**

Open `src/app/api/expense-transactions/[id]/route.test.ts`.

- If a test exists that asserts "system-generated transactions are deleted without reversing balance" (search the file for `isSystemGenerated: true` around lines 240–260) — delete that test, it encodes the bug.
- Add a new test: `'reverses account balance when deleting a system-generated child payment'`. It mocks `db.expenseTransaction.findUnique` to return `{ ...mockExistingExpense, isSystemGenerated: true, isInstallment: false, parentInstallmentId: 'parent-123' }`, invokes DELETE, and asserts `db.financialAccount.update` is called with `data: { currentBalance: { increment: <Number(mockExistingExpense.amount)> } }` and that `db.$transaction` was called once.
- Keep the existing `'rejects parent installment delete with 400'` test unchanged.
      </action>
      <verify>
Run `npx vitest run src/app/api/expense-transactions/[id]/route.test.ts`. All tests in this file must pass, including the newly added one. Then run `npm run lint` — must exit 0.
      </verify>
    </task>

  </phase>

  <phase name="Phase 2 — New desktop Sheet editor (Fix 2)">

    <task>
      <name>Create EditInstallmentSheet component</name>
      <action>
Create a new file `src/components/installments/EditInstallmentSheet.tsx`.

Base its structure on `src/components/forms/EditAccountSheet.tsx`. Copy the top-level shape (Sheet + SheetContent + isFetching / error / Form branches, SheetFooter, Separator, delete button, DeleteDialog sibling).

Props:

```ts
interface EditInstallmentSheetProps {
  open: boolean;
  onOpenChange: (open: boolean) => void;
  className?: string;
  installmentId: string;
}
```

Data:

- `const { updateInstallment, deleteInstallment, isUpdating, isDeleting } = useInstallmentsQuery();`
- `const { installmentData, isFetching, error } = useInstallmentQuery(installmentId, { enabled: open });`
- `const { budgets } = useExpenseTypesQuery();`
- `const [deleteDialogOpen, setDeleteDialogOpen] = useState(false);`

Form:

- `useForm` with `resolver: zodResolver(updateInstallmentSchema)`, `mode: "onTouched"`, default values matching the existing `EditInstallmentDrawer`.
- `useEffect` to `form.reset({...})` when `installmentData` loads, mirroring the Drawer's reset payload (name, amount, installmentStartDate as Date, expenseTypeId, expenseSubcategoryId defaulting to `"none"` when null).

Render branches:

1. `isFetching` → `<SkeletonEditAccountSheetForm />` (reuse).
2. `error` → error block copied from `EditAccountSheet` (title "Unable to load installment", description from error, Try again + Close buttons).
3. Otherwise → `<Form>` + `<form onSubmit={form.handleSubmit(onSubmit)}>` with:
   - `SheetHeader` with `SheetTitle="Edit installment"`, `SheetDescription="Update your installment details."`.
   - `FormField` for `name` — `Input`, `FormItem className="p-4"`.
   - `FormField` for `amount` — `Input type="number"` with spinner-hidden classes (copy from `EditAccountSheet` initialBalance field). Underneath, a live `FormDescription` that reads: `Monthly: ₱{((parseFloat(form.watch('amount') || '0')) / (installmentData?.installmentDuration ?? 1)).toLocaleString('en-PH', { minimumFractionDigits: 2, maximumFractionDigits: 2 })}`.
   - A read-only Account display — label "Account", `Input value={installmentData?.account?.name ?? ''} disabled className="bg-muted text-muted-foreground cursor-not-allowed"`.
   - A read-only Duration display — label "Duration", `Input value={`${installmentData?.installmentDuration ?? 0} months`} disabled className="bg-muted text-muted-foreground cursor-not-allowed"`.
   - `FormField` for `installmentStartDate` — Popover + `Calendar` (no `disabled` prop on Calendar). Copy the trigger markup from `EditInstallmentDrawer`.
   - `FormField` for `expenseTypeId` — `Select` populated from `budgets`. On change, also reset `expenseSubcategoryId` to `"none"`.
   - `FormField` for `expenseSubcategoryId` — conditionally rendered only when the selected expense type has subcategories. Uses `"none"` sentinel.
   - `SheetFooter` with:
     - `<Button type="submit" disabled={isUpdating}>{isUpdating ? "Updating installment..." : "Update installment"}</Button>`
     - `<SheetClose asChild><Button variant="outline" disabled={isUpdating}>Cancel</Button></SheetClose>`
   - `<Separator className="mt-2 mb-6" />`.
   - `<div className="px-4 pb-4"><Button type="button" variant="outline" className="w-full text-error-700 hover:text-error-600 hover:bg-error-50 border-error-300" onClick={() => setDeleteDialogOpen(true)} disabled={isUpdating || isDeleting}>{isDeleting ? "Deleting..." : "Delete installment"}</Button></div>`.

Submit handler `onSubmit`:

```ts
const onSubmit = async (values) => {
  try {
    await updateInstallment({
      id: installmentId,
      name: values.name,
      amount: values.amount,
      installmentStartDate: formatDateForAPI(values.installmentStartDate),
      expenseTypeId: values.expenseTypeId,
      expenseSubcategoryId: values.expenseSubcategoryId === 'none' ? null : values.expenseSubcategoryId,
    });
    toast.success("Installment updated successfully", { duration: 5000 });
    form.reset();
    onOpenChange(false);
  } catch (error) {
    toast.error("Failed to update installment", {
      description: error instanceof Error ? error.message : "Please check your information and try again.",
      duration: 6000,
    });
  }
};
```

Delete handler `handleDeleteConfirm`:

```ts
const paidCount = (installmentData?.installmentDuration ?? 0) - (installmentData?.remainingInstallments ?? 0);

const handleDeleteConfirm = () => {
  toast.success("Installment deleted successfully", { duration: 5000 });
  deleteInstallment(installmentId);
  setDeleteDialogOpen(false);
  onOpenChange(false);
};
```

At the bottom of the component's return, after `</Sheet>`, render:

```tsx
<DeleteDialog
  open={deleteDialogOpen}
  onOpenChange={setDeleteDialogOpen}
  onConfirm={handleDeleteConfirm}
  title="Delete installment"
  description={`This will permanently delete this installment and all ${paidCount} payment records. This cannot be undone.`}
  isDeleting={isDeleting}
/>
```

Wrap the top-level return in a fragment (`<>...</>`). Export as default.

**Test:** Create `src/components/installments/EditInstallmentSheet.test.tsx` mirroring `EditInstallmentDrawer.test.tsx`:
- Renders `SkeletonEditAccountSheetForm` while `isFetching`.
- Renders form when data loads; `name` field prefilled.
- Account and Duration rendered as disabled `Input`s.
- Submitting calls `updateInstallment` with the expected payload.
- Clicking "Delete installment" opens the `DeleteDialog`; clicking Confirm calls `deleteInstallment(installmentId)` and fires `toast.success`.
      </action>
      <verify>
Run `npx vitest run src/components/installments/EditInstallmentSheet.test.tsx`. Must pass. Then `npm run lint` and `npm run build` — both must exit 0.
      </verify>
    </task>

  </phase>

  <phase name="Phase 3 — Desktop table structural rewrite (Fix 1 + Fix 2 wiring)">

    <task>
      <name>Rewrite InstallmentTable with full ExpenseTable shell</name>
      <action>
Rewrite `src/components/installments/InstallmentTable.tsx`. Keep the file path; fully replace its content.

**Imports** (compose from ExpenseTable):

```ts
import React, { useEffect, useMemo, useState } from 'react';
import { addDays, format } from 'date-fns';
import { IconEdit } from '@tabler/icons-react';
import { ChevronLeft, ChevronRight, SearchIcon, SearchX } from 'lucide-react';
import {
  createColumnHelper,
  flexRender,
  getCoreRowModel,
  getPaginationRowModel,
  useReactTable,
} from '@tanstack/react-table';
import { Badge } from '@/components/ui/badge';
import { Button } from '@/components/ui/button';
import { EmptyState } from '@/components/shared/EmptyState';
import { InputGroup, InputGroupAddon, InputGroupInput } from '@/components/ui/input-group';
import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from '@/components/ui/select';
import { Table, TableBody, TableCell, TableHead, TableHeader, TableRow } from '@/components/ui/table';
import { ToggleGroup, ToggleGroupItem } from '@/components/ui/toggle-group';
import { type Installment } from '@/hooks/useInstallmentsQuery';
```

**Props:**

```ts
interface InstallmentTableProps {
  installments: Installment[];
  onEdit: (id: string) => void; // delete no longer lives on the row
}
```

**Helpers** (keep `formatCurrency` and `computeNextPayment` from the current file).

**Component body:**

1. State: `searchTerm`, `debouncedSearchTerm`, `dateFilter` (default `'view-all'`).
2. Debounce effect (300 ms) copied from `ExpenseTable`.
3. `DATE_FILTER_OPTIONS = { viewAll: 'view-all', thisMonth: 'this-month', thisYear: 'this-year' } as const;`
4. `filteredData = useMemo(...)` that filters `installments` by:
   - Date filter on `row.installmentStartDate`:
     - `view-all` → no filter.
     - `this-month` → month + year match `new Date()`.
     - `this-year` → year matches `new Date()`.
   - Search (case-insensitive) across `row.name`, `row.expenseType?.name`, `row.expenseSubcategory?.name`, `row.account?.name`.
5. Columns using `createColumnHelper<Installment>()` — **8 columns**:
   - `name`: header "Name". Cell renders `<span className="font-semibold max-w-[200px] block truncate">{row.original.name}</span>`.
   - `amount`: header "Total", right-aligned. Cell `<div className="text-right">₱{formatCurrency(row.original.amount)}</div>`.
   - `monthlyAmount`: header "Monthly", right-aligned. `<div className="text-right">₱{row.original.monthlyAmount ? formatCurrency(row.original.monthlyAmount) : '—'}</div>`.
   - `installmentDuration`: header "Progress". Cell computes `paid` / `duration` and renders the existing progress-bar markup (copy verbatim from the current file including role="progressbar").
   - `installmentStartDate`: header "Start date". Cell `row.original.installmentStartDate ? format(new Date(row.original.installmentStartDate), 'MMM d, yyyy') : '—'`.
   - display column `id: 'nextPayment'`: header "Next payment". Computes via `computeNextPayment`; if `installmentStatus === 'COMPLETED'` renders `'—'`.
   - `installmentStatus`: header "Status". Active → `<Badge variant="secondary">Active</Badge>`, Completed → `<Badge variant="outline">Completed</Badge>`.
   - display column `id: 'actions'`: header "Actions", right-aligned. Cell:
     ```tsx
     <div className="flex justify-end gap-1">
       <Button
         variant="ghost"
         size="icon"
         aria-label="Edit installment"
         onClick={() => onEdit(row.original.id)}
       >
         <IconEdit className="h-4 w-4" />
       </Button>
     </div>
     ```
   Set `columns` in a `useMemo`.
6. `useReactTable` with `{ data: filteredData, columns, getCoreRowModel, getPaginationRowModel, autoResetPageIndex: false, initialState: { pagination: { pageSize: 10 } } }`.
7. Empty-page safety `useEffect` (copy from `ExpenseTable` — clamps `pageIndex` when data shrinks).
8. `PAGE_SIZE_OPTIONS = [10, 20, 30, 40, 50] as const;` and `getPageNumbers` helper (copy verbatim).

**JSX** (copy the wrapping structure from `ExpenseTable`'s return — top toolbar with ToggleGroup + InputGroup, the `<div className="overflow-hidden rounded-md border">` Table wrapper, pagination strip). Change only:

- Table header cells use the 8 headers defined above with `className="p-4"` (right-aligned variants use `className="p-4 text-right"`).
- Body cells use `className="pl-4"` (copy from ExpenseTable).
- ToggleGroup renders 3 items only (View all / This month / This year) — no "This week" item.
- `InputGroupInput` placeholder: `Search installments...`.
- Table-level empty state (`filteredData.length === 0` AND search/filter applied): `EmptyState` with `icon={SearchX}`, `title="No installments found"`, `description="Try adjusting your search or filters."`, `variant="table"` rendered inside `<TableCell colSpan={8}>`.
- Pagination "Showing N out of M" text reads the filtered count — identical logic to ExpenseTable.
- No `DeleteDialog` inside this component. No trash button anywhere.

Export as default.

**Test updates:** Rewrite `src/components/installments/InstallmentTable.test.tsx`:
- Render with a multi-row fixture.
- Assert search input is present (`placeholder="Search installments..."`); typing filters rows after debounce.
- Assert three ToggleGroupItems are present (View all, This month, This year); activating "This year" filters by `installmentStartDate`.
- Assert pagination controls are present.
- Assert pencil button click calls `onEdit(id)`.
- Assert there is no "Delete" button / trash icon in the table.
- Assert the `SearchX` empty state renders when the query matches nothing.
      </action>
      <verify>
Run `npx vitest run src/components/installments/InstallmentTable.test.tsx`. Must pass. Then `npm run lint` and `npm run build` — both must exit 0.
      </verify>
    </task>

  </phase>

  <phase name="Phase 4 — Mobile card cleanup (Fix 3)">

    <task>
      <name>Strip action buttons from InstallmentCard and switch to onClick prop</name>
      <action>
Edit `src/components/installments/InstallmentCard.tsx`.

1. Props — replace with:

```ts
interface InstallmentCardProps {
  installment: Installment;
  onClick?: () => void;
}
```

Remove `onEdit` and `onDelete`.

2. Imports — remove `Trash2` (from `lucide-react`), `IconEdit` (from `@tabler/icons-react`), and `Button` (from `@/components/ui/button`). Keep `Icons`, `Badge`, date/format helpers.

3. Root div — change `onClick={() => onEdit(installment.id)}` to `onClick={onClick}`. Keep `className="money-map-card-interactive flex flex-col gap-3 cursor-pointer"`.

4. Delete the entire bottom block:

```tsx
<div className="flex justify-end gap-1 pt-2">
  <Button variant="ghost" size="icon" aria-label="Edit installment" onClick={(e) => { e.stopPropagation(); onEdit(installment.id); }}>
    <IconEdit className="h-4 w-4" />
  </Button>
  <Button variant="ghost" size="icon" aria-label="Delete installment" onClick={(e) => { e.stopPropagation(); onDelete(installment.id, installment.name); }}>
    <Trash2 className="h-4 w-4" />
  </Button>
</div>
```

Nothing else changes. The status badge, info grid, and progress bar are untouched.

**Test updates** — rewrite `src/components/installments/InstallmentCard.test.tsx`:
- Remove mock `onEdit`, `onDelete` props.
- Add `onClick` mock.
- Assert clicking the card root calls `onClick` once.
- Remove any assertions that query for Edit / Delete buttons (they should not exist).
- Keep assertions on name, monthly amount, progress bar, status badge.
      </action>
      <verify>
Run `npx vitest run src/components/installments/InstallmentCard.test.tsx`. Must pass. Then `npm run lint` — must exit 0.
      </verify>
    </task>

  </phase>

  <phase name="Phase 5 — Tab container rewire (Fix 3 wiring + Fix 4)">

    <task>
      <name>Wire Sheet + Drawer split in InstallmentsTabContent and remove tab-level delete</name>
      <action>
Edit `src/components/installments/InstallmentsTabContent.tsx`.

1. Imports:
   - Add `import EditInstallmentSheet from '@/components/installments/EditInstallmentSheet';`
   - Remove `import DeleteDialog from '@/components/shared/DeleteDialog';` (no longer used here).

2. State — remove:
   - `deleteDialogOpen`, `setDeleteDialogOpen`
   - `installmentToDelete`, `setInstallmentToDelete`
   - `handleDeleteRequest`
   - `handleDeleteConfirm`
   Also remove `deleteInstallment` and `isDeleting` from the `useInstallmentsQuery({...})` destructure (the editors pull these themselves).

3. Rename `editDrawerOpen` / `setEditDrawerOpen` → `editOpen` / `setEditOpen` for clarity (optional but preferred; if renaming is skipped, leave variable names but they must still drive both Sheet and Drawer).

4. `handleEdit` unchanged — `setSelectedInstallmentId(id); setEditOpen(true);`.

5. Mobile card list — change `<InstallmentCard key={...} installment={i} onEdit={handleEdit} onDelete={handleDeleteRequest} />` to `<InstallmentCard key={i.id} installment={i} onClick={() => handleEdit(i.id)} />`.

6. Desktop table — change `<InstallmentTable installments={installments} onEdit={handleEdit} onDelete={handleDeleteRequest} />` to `<InstallmentTable installments={installments} onEdit={handleEdit} />`.

7. Editors — replace the single `EditInstallmentDrawer` mount with a responsive split:

```tsx
<div className="md:hidden">
  <EditInstallmentDrawer
    open={editOpen}
    onOpenChange={setEditOpen}
    installmentId={selectedInstallmentId}
  />
</div>
<div className="hidden md:block">
  <EditInstallmentSheet
    open={editOpen}
    onOpenChange={setEditOpen}
    installmentId={selectedInstallmentId}
  />
</div>
```

8. Delete `<DeleteDialog ... />` block entirely.

9. Open `src/components/installments/EditInstallmentDrawer.tsx`. In its `handleDeleteConfirm` (or equivalent), ensure the first line is:

```ts
toast.success("Installment deleted successfully", { duration: 5000 });
```

followed by `deleteInstallment(installmentId); setDeleteDialogOpen(false); onOpenChange(false);`. If the toast is already present, leave it. If the order is different, match the `ExpenseTable.handleDeleteConfirm` pattern (toast.success BEFORE the mutation call, since the mutation is fire-and-forget).

**Test updates:**

- `src/components/installments/EditInstallmentDrawer.test.tsx` — if it asserts on toast behavior, update to assert that `toast.success` is called with exactly `"Installment deleted successfully"` when the delete is confirmed.
- If an `InstallmentsTabContent.test.tsx` exists, update it to reflect the removed `DeleteDialog` and the new Sheet/Drawer split. If it does not exist, no change.
      </action>
      <verify>
Run the targeted tests:

```
npx vitest run src/components/installments/InstallmentsTabContent.test.tsx
npx vitest run src/components/installments/EditInstallmentDrawer.test.tsx
```

(Skip files that don't exist.)

Then run the full suite: `npx vitest run`. Must be green.

Then `npm run lint` and `npm run build` — both must exit 0.

Finally, update `docs/installment-management-verification.md` by appending a new section `## QA Fix Pass` that documents:

- Each of the 5 fixes, file(s) touched, test(s) added/modified, and the verification command that confirmed green.
- Manual smoke test results for steps 1–9 in spec §12.3, especially step 9 (child delete balance reversal).
      </verify>
    </task>

  </phase>

</plan>
