/**
 * Shared "confident" ease-out curve (ease-out-quint) for framer-motion
 * transitions. Mirrors the --ease-out-quint CSS token in globals.css so
 * JS-driven and CSS-driven motion share one easing language.
 */
export const EASE_OUT_QUINT: [number, number, number, number] = [0.22, 1, 0.36, 1];
